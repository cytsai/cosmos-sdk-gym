helios:
    env: env.CosmosSDKEnv
    run: IMPALA
    stop:
        timesteps_total: 1000000
    config:
        #log_level: DEBUG
        framework: torch
        num_workers: 8
        num_gpus: 1
        model:
            use_lstm: True
            lstm_use_prev_action: True
            #max_seq_len: 200
